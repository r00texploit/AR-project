name: Diagnostics

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Runner is alive — Actions is working"

      - name: Show environment
        run: uname -a && echo "GitHub Actions OK"
